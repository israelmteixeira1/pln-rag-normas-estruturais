# =============================================================================
# RAG Normas Estruturais — NBR 6120
# =============================================================================
# Instalação: pip install -r requirements.txt
# Para Google Colab: !pip install -r requirements.txt

# --- Embeddings (PT-BR técnico) ---
sentence-transformers>=3.0.0     # modelo neuralmind/bert-base-portuguese-cased
torch>=2.0.0                     # backend PyTorch para sentence-transformers

# --- Índice Vetorial ---
faiss-cpu>=1.9.0               # wheels pré-compilados disponíveis a partir de 1.9.0

# --- Buscas Híbridas (BM25 + Embedding) ---
rank-bm25>=0.2.2                # BM25 para busca léxica (hybrid search)

# --- Utilitários ---
numpy>=1.26.0                  # faiss >= 1.9 é compatível com NumPy 2.x
pandas>=2.0.0
tqdm>=4.66.0

# =============================================================================
# Fase 3 — Pipeline RAG e Interface (Trilha E)
# =============================================================================

# --- LLM ---
google-genai>=1.0.0           # API client para Google Gemini (fallback)
groq>=0.9.0                   # API client para Groq (provider padrão)
openai>=1.0.0                 # OpenAI SDK — usado pelo NVIDIA NIM (OpenAI-compatible)

# --- Interface Web ---
gradio>=4.0.0                 # interface do chatbot (inline no notebook)

# --- Configuração ---
python-dotenv>=1.0.0          # carregamento de .env (API keys)

# --- Desenvolvimento / Notebooks ---
notebook>=7.0.0               # Jupyter Notebook
